----Annotation---
@RunWith(SpringRunner.class)
@SpringBootTest
@ContextConfiguration(classes={TaxService.class, TaxBracketService.class}) // only these classes will be initialised

@Mock
NoSQLDataParser classObj ;
	
@Rule 
public MockitoRule mockitoRule = MockitoJUnit.rule(); 

@Test