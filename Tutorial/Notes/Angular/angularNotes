node –v & npm -v

npm install -g @angular/cli

ng -v

ng new mytutorial
	This will create a skeleton Angular 2 application. You can check the default folder structure.

ng new my_first_angular_app --style=scss 
	if you want to create an Angular project using SASS, you need to run the following command:

ng serve 
	ng serve command launches the server, watches our files, and rebuilds the app as you make changes to the files.
http://localhost:4200} run at this.


ng generate class user
ng generate service user-service
ng generate component user-list

Reference
	https://www.baeldung.com/spring-boot-angular-web
	


Commands 
    ng new angular-tour-of-heroes : cleating new app
    ng serve --open } open the application
        The ng serve command builds the app, starts the development server, watches the source files, and rebuilds the app as you make changes to those files.
        The --open flag opens a browser to http://localhost:4200/.

The double curly braces are Angular's interpolation binding syntax. <h1>{{title}}</h1>
